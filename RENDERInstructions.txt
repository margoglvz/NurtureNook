one folder for frontend
serve static files

static_folder ='static', stat
move frontend files to a static folder

when run flask, test it by going to local port number and check if the files are loading

on render, provide the command to run flask 
"python app.py or something"

